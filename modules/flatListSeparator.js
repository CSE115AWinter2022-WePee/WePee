import { View } from 'react-native'

  // Separates the flatList, is a single pixel line
  export const genericFlatListSeparator = () => (<View
    style={{
      height: 1,
      backgroundColor: '#CED0CE',
      marginLeft: '5%',
      marginRight: '5%'
    }}
                                   />
  )